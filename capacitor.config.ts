import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Avaliação_PDM',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
